# scgnn-api runtime dependencies (the web service layer).
#
# The scgnn inference package is VENDORED into this repo (the top-level scgnn/
# directory) - there is NO install from GitHub and no GH_TOKEN. The heavy model
# stack (CPU torch, torch-geometric, transformers/CodeBERT, scikit-learn,
# slither-analyzer) and the solc-select compiler superset are installed by the
# Dockerfile, pinned to the training-lock versions, with CPU torch first so the
# CUDA build is never pulled. They are intentionally not pinned here, to keep
# this file the web layer's own surface.

fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
huggingface_hub>=0.24,<2
python-multipart>=0.0.9