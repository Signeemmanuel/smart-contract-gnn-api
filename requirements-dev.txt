# Light dev/test dependencies. The scgnn package is VENDORED (scgnn/ in the repo),
# so nothing is installed for it; the suite touches only the stdlib-only
# scgnn.schema and mocks the heavy calls. No torch, no Slither, no Hub.
#
#   pip install -r requirements-dev.txt
#   pytest -q

fastapi>=0.115,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
huggingface_hub>=0.24,<2
python-multipart>=0.0.9
pytest>=8
httpx>=0.27