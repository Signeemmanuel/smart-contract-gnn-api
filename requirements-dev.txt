# Light dev/test dependencies. The test suite mocks the heavy calls
# (analyze_source / load_model) and revision resolution, so it does NOT need
# torch, Slither or the Hub.
#
# It does need `scgnn.schema` (stdlib-only) importable. Install the pinned scgnn
# package WITHOUT its heavy dependencies for a light dev/test environment:
#
#   pip install --no-deps "scgnn @ git+https://github.com/Signeemmanuel/smart-contract-gnn-model.git@23ffacc1c145a1c79788ea2cd791d5b0dc0ced2b"
#   pip install -r requirements-dev.txt
#   pytest -q

fastapi>=0.115,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
huggingface_hub>=0.24,<2
python-multipart>=0.0.9
pytest>=8
httpx>=0.27
